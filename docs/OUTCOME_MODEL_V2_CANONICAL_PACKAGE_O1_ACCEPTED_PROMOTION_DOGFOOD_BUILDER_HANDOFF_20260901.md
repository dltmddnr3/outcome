# OUTCOME Model v2 canonical package O1 — accepted promotion and final dogfood Builder handoff

Outcome: preserve every unrelated active-root byte while promoting the exact Cherry-accepted O1 Audit lineage, then consume exactly one current selective-context plan locally and close only O1 with immutable evidence.

Canonical Gate: `GATES_OUTCOME_MODEL_V2_CANONICAL_PACKAGE_PROMOTION_20260901.md#O1`.

Authority: Cherry explicitly accepted Audit carrier `e912c61ac718165e864a5e89478fa4690d11aa72` and authorized one dirty-aware local promotion, one final dogfood consumption and O1 Gate closure. External activation, Preview, Production, deployment, release and Phase completion remain excluded.

## Immutable inputs

- Active root / branch / HEAD: `/Users/rosum/Documents/ChatGPT/OUTCOME` / `codex/hp1-session-bearer` / `75e449de24b01e56df7b896cd2b89e849df17efe`.
- Unrelated active-root dirty baseline after excluding all named Planner O1 handoffs/checkpoints: `396` entries / `94026362dc61e27549a8b9ed9fb620d3751a734b353978d0d875148b4df3f48f`; staged entries `0`.
- Release Audit carrier/tree/parent: `e912c61ac718165e864a5e89478fa4690d11aa72` / `a727dd06c1f24a023bdd579b78c90ff2104b7aab` / `62ddcec5c9c42a5219bb721e4a271c8d27428913`.
- Release Audit receipt SHA-256: `b344355ca80d8245fa62ecfd2e3e257b1ec22fdf1b086e26fb1154e378ef08f8`.
- Cherry acceptance path: `docs/OUTCOME_MODEL_V2_CANONICAL_PACKAGE_O1_CHERRY_ACCEPTANCE_RECEIPT_20260901.md`; verify routed SHA-256 before work.
- This handoff path: `docs/OUTCOME_MODEL_V2_CANONICAL_PACKAGE_O1_ACCEPTED_PROMOTION_DOGFOOD_BUILDER_HANDOFF_20260901.md`; verify routed SHA-256 before work.
- Protected Builder binding: exact current `outcome/builder` binding version `18`; require exact self-match, doctor clean and lock clear immediately before every mutation boundary.

## Dirty-aware promotion

1. Re-pin active root, branch, Audit ancestry, acceptance/handoff bytes, Builder binding, staged state and a private content-addressed manifest of every dirty tracked/index/untracked path with path class, mode and digest.
2. In the isolated Builder worktree create a linear acceptance carrier above exact Audit carrier `e912c61a...` by adding only the exact Cherry acceptance receipt and this handoff. No merge, cherry-pick, rewrite or product amendment.
3. Compute every transition path from active HEAD to the acceptance carrier and intersect it with the complete dirty manifest. Only `exact_target_bytes` intersections may be transactionally quarantined. Any nonidentical, mode/type/symlink/index ambiguity is `SAFE_HOLD` with active-root mutation zero.
4. For exact-target untracked collisions only, use a private temporary recovery directory and exact manifest; move no unrelated path. Run exactly one non-interactive `git merge --ff-only <acceptance-carrier>` in the active root. On pre-advance failure restore exact bytes/modes and stop; if the ref advances but verification is ambiguous, return `BLOCKED` without reset.
5. Verify active root equals the acceptance carrier, all target bytes match, every unrelated manifest entry and mode remains identical, index remains empty, and merge/rebase/cherry-pick/lock/recovery residue is zero.

## Exactly one final dogfood consumption

6. From the verified active root, invoke the committed local canary exactly once with `OUTCOME_MODEL_V2_ENABLED=1` and its required explicit O1 local probe authority. No retry or replay.
7. Require one validated current plan and one capable in-memory adapter callback: outcome `o1_local_dogfood_probe_consumed`, local consumption `1`, callback `1`, receipt `1`, duplicate `0`, retry `0`, execution-started `0`, persistent-setting `0`, registry/provider/environment `0`, unauthorized transition `0`, false completion `0`.
8. Require projection `7/8`, frontier `milestone-o1`, selected next action `work-o1-selective-context-dogfood`, Cherry action and active work null, rollback available, six loaded/six skipped source classes, sole Builder role skill `mango-implementation-engineer`, empty expansion allowlist/expansions and null handoff.
9. Require exact accepted digests unless the committed compiler deterministically proves a content-addressed Gate-only update: projection `ba9cd29d81081c04bea7a8193e87b2f96cf70a86ba28402a0eb2c1b3daa523ea`, projection source manifest `b54ffd4e5c6bc4912d589993ab50db208494390bc8c36f80a65264f4a416993c`, corrected selector manifest `a5e39938e12bd15dd1c24576070ca70842309cee643ec2b7c7ed024e1f255361`, snapshot `7da833943f17138e6d86bf6763bff4fb9212c3f53c15124d6f8c9e721a3bf295`, plan `3fc48b99b0b6bd560bc2b28a182cedcbfd266b8b3fdb96a685fa972fa159031a`. Any unexplained drift is `SAFE_HOLD`; do not recompile or retry automatically.
10. Scan the public receipt payload for local paths, private task/thread/session/turn locators, raw prompts/results, credentials, registry payload and user-owned Contract/Map bytes; hits must be zero.

## O1 evidence closure and final carrier

11. In the isolated Builder branch above the acceptance carrier, add only:
   - updated `GATES_OUTCOME_MODEL_V2_CANONICAL_PACKAGE_PROMOTION_20260901.md`, changing O1 from pending to checked with exact consumption evidence and truthful status; and
   - `docs/OUTCOME_MODEL_V2_CANONICAL_PACKAGE_O1_FINAL_DOGFOOD_BUILDER_RECEIPT_20260901.md`.
12. O1 closure must state that local selection/consumption succeeded exactly once and cannot imply Phase 3 completion, external activation, deployment or release. R1/R2 and prior evidence remain unchanged.
13. Re-run the focused/core correction suite and `git diff --check`. Reverify active-root dirty manifest and binding immediately before the final mutation.
14. Fast-forward the active root exactly once to the final O1 receipt carrier only if the two-path final delta has no nonidentical dirty collision. Verify branch/HEAD/tree/parent, Gate/receipt hashes, unrelated manifest parity, index zero, operation residue zero and no external/runtime mutation.
15. Publish the final carrier under a durable local branch in the canonical OUTCOME object database.

## Rollback and terminal contract

Before promotion, mutation is zero. After a verified linear promotion, do not move the branch backward; preserve all evidence and use a separately authorized history-preserving revert or later forward correction. Dogfood state is in-memory only and needs no persistent cleanup; disabling or unsetting `OUTCOME_MODEL_V2_ENABLED` plus process restart preserves the verified rollback boundary.

Return exactly `O1_DOGFOOD_COMPLETE`, `SAFE_HOLD`, or `BLOCKED`.

`O1_DOGFOOD_COMPLETE` must include acceptance carrier and final carrier commit/tree/parent, Gate and receipt SHA-256, changed paths, transition/intersection counts, pre/post unrelated manifest, exact consumption/callback/receipt/duplicate and safety counters, projection/source/snapshot/plan digests, test counts, active-root readback, durable branch, rollback and residual exclusions.
